# Utils Directory

Utility functions and helper modules that can be reused across different parts of the backend application.

Examples of upcoming files:
- `tokenHelper.js`: Generates and verifies JSON Web Tokens (JWT).
- `validator.js`: Validates CGPA, email formats, and required input fields.
